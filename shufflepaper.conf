#!/bin/bash
# shufflepaper.conf
# Stores variables useful for shufflepaper

dataDir="${XDG_DATA_HOME:="$HOME/.local/share"}/shufflepaper"
wallDB="$dataDir/walls.db"
installDir="$(dirname "${BASH_SOURCE[0]}")"
uncategorized=9
userConf="$dataDir/user.conf"
. "$userConf"
